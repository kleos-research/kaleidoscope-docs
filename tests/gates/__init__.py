# Present so that `python3 -m unittest discover -s tests` descends into this
# directory. Discovery does not recurse into a directory that is not a package,
# and a test nobody runs is a gate nobody checks.
